/**
 * Drizzle schema barrel. Domain tables (restaurants, tables, menu, orders, ...) land
 * here in US-002 (data model + migrations). Keeping the export here lets the Drizzle
 * client and drizzle-kit config point at a single, stable module path from day one.
 */
export {}
